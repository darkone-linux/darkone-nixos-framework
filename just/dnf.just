# DNF just — framework bootstrap, imported by `dnf/Justfile`.
#
# Framework sources only: tests, fixtures, package bumps, framework release.

import 'common.just'
import 'dnf/dev.just'
import 'dnf/release.just'
import 'dnf/test.just'
